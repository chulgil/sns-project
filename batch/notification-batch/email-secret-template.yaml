apiVersion: v1
kind: Secret
metadata:
  name: email-secret
  namespace: sns
type: Opaque
data:
  # 실제 값은 환경변수나 별도 관리 시스템에서 설정
  # 예: echo -n "your-smtp-user" | base64
  SMTP_USER: <base64-encoded-smtp-user>
  SMTP_PASSWORD: <base64-encoded-smtp-password> 