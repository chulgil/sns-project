apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: sns
type: Opaque
data:
  # 실제 값은 환경변수나 별도 관리 시스템에서 설정
  # 예: echo -n "your-username" | base64
  MYSQL_USER: <base64-encoded-username>
  MYSQL_PASSWORD: <base64-encoded-password> 